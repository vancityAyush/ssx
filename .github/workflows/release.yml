name: Create Release with Binary and Notes

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version (e.g. v2.1.0)'
        required: true
        type: string
      prerelease:
        description: 'Mark as pre-release'
        required: false
        default: 'false'
        type: choice
        options:
          - 'false'
          - 'true'

jobs:
  create-tag:
    if: github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Create and push tag
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag "${{ github.event.inputs.version }}"
          git push origin "${{ github.event.inputs.version }}"

  release:
    needs: [create-tag]
    if: always() && (needs.create-tag.result == 'success' || needs.create-tag.result == 'skipped')
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - name: Resolve tag name
      id: tag
      run: |
        if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
          echo "name=${{ github.event.inputs.version }}" >> "$GITHUB_OUTPUT"
        else
          echo "name=${{ github.ref_name }}" >> "$GITHUB_OUTPUT"
        fi

    - name: Checkout code
      uses: actions/checkout@v4
      with:
        ref: ${{ steps.tag.outputs.name }}

    - name: Prepare binary assets
      run: |
        chmod +x ssh.sh
        mkdir -p release
        cp ssh.sh release/ssh-key-generator.sh
        cp ssh.ps1 release/ssh-key-generator.ps1

    - name: Generate release notes
      run: |
        cat > release_notes.md << 'EOF'
        # SSH Key Generator Script

        This release contains the SSH key generation scripts that automate the process of creating and configuring SSH keys for Bitbucket, GitHub, GitLab, or Azure DevOps.

        ## Quick Usage

        ### Bash / Zsh
        Run the script directly without downloading:
        ```bash
        curl -sSL https://raw.githubusercontent.com/${{ github.repository }}/main/ssh.sh | bash
        ```

        Or download and run locally:
        ```bash
        curl -O https://raw.githubusercontent.com/${{ github.repository }}/main/ssh.sh
        chmod +x ssh.sh
        ./ssh.sh
        ```

        ### PowerShell
        ```powershell
        Invoke-WebRequest -Uri "https://raw.githubusercontent.com/${{ github.repository }}/main/ssh.ps1" -OutFile "ssh.ps1"
        ./ssh.ps1
        ```

        ## What's Included

        - **ssh-key-generator.sh**: Bash script for macOS, Linux, WSL, Git Bash
        - **ssh-key-generator.ps1**: PowerShell script for Windows, Linux, macOS

        ## Features

        - Arrow-key interactive menu for provider selection
        - Ed25519 key generation for all providers
        - Optional git username and email configuration
        - Automatic SSH key generation with appropriate algorithms
        - SSH agent configuration
        - SSH config file management
        - Public key clipboard copying (when supported)
        - Cross-platform browser opening to platform SSH settings pages
        - SSH connection testing

        ## Platform Support

        | Platform | SSH Agent | Clipboard | Browser Opening |
        |----------|-----------|-----------|----------------|
        | macOS    | ✅        | pbcopy    | open           |
        | Linux    | ✅        | xclip / xsel | xdg-open    |
        | Windows  | ✅        | clip.exe  | start          |
        | WSL      | ✅        | clip.exe / xclip | xdg-open |

        ## Requirements

        - Bash shell
        - SSH client utilities
        - Internet connection for key testing
        EOF

        echo "Generated release notes:"
        cat release_notes.md

    - name: Upload Release Assets
      uses: softprops/action-gh-release@v1
      with:
        tag_name: ${{ steps.tag.outputs.name }}
        files: |
          release/ssh-key-generator.sh
          release/ssh-key-generator.ps1
          ssh.sh
          ssh.ps1
        body_path: release_notes.md
        name: "SSH Key Generator ${{ steps.tag.outputs.name }}"
        draft: false
        prerelease: ${{ github.event.inputs.prerelease == 'true' || false }}
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
